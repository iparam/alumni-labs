<%= nested_form_for(@university_profile,:html=>{:class=>"form-horizontal"}) do |f| %>
<fieldset>
    <legend>University Information</legend>
  <% if @university_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@university_profile.errors.count, "error") %> prohibited this university_profile from being saved:</h2>

      <ul>
      <% @university_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name,:class=>"control-label" %>
    <div class="controls">
      <%= f.text_field :name %>
     </div> 
  </div>
  <div class="control-group">
    <%= f.label :address,:class=>"control-label" %>
    <div class="controls">
      <%= f.text_area :address,:rows=>5 %>
    </div>  
  </div>
  <div class="control-group">
    <%= f.label :summary,:class=>"control-label" %>
    <div class="controls">
      <%= f.text_area :summary ,:rows=>5%>
    </div>  
  </div>
  <%= f.fields_for :university_courses do |courses| %>
   <%=render :partial => "university_courses_field", :locals => {:builder => courses } %>
  <% end %>
  <%= f.link_to_add "Add a course", :university_courses,:class=>"btn" %></p>
  <div class="form-actions">
    <%= f.submit "Save",:class=>"btn btn-primary" %>
  </div>
  </fieldset>
<% end %>
