<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="#">Alumnii Labs</a>
      <div class="nav-collapse">
        <ul class="nav">
          <li class="<%= active_nav_class('home') %>"><%= link_to raw("<i class='icon-home icon-white'></i> Home"), root_path %></li>
        </ul>

        

        <p class="navbar-text pull-right">
          <% if current_user %>
            Logged in as <b><%= current_user.name || urrent_user.email%></b>
            |
            <a href="<%=logout_path%>" class='white-link'>Logout</a>
          <% else %>
            <%= link_to "Sign up", signup_path, :class => 'white-link'%>
            |
            <%= link_to "Login", login_path, :class => 'white-link'%>
          <% end %>
        </p>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>

